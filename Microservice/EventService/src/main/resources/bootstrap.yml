server:
  port: 3002
boot:
    test: ${test:http://localhost1234}
spring:
  application:
    name: eventservice
  profiles:
    active: dev
  data:
      mongodb:
#          host: ${mongodb_host:localhost}
#          port: 27017
          uri: mongodb+srv://meetuAdmin:popsonfonmeetu@cluster0-uuw7c.mongodb.net/test?retryWrites=true&w=majority
          authentication-database: admin
          database: meetu
#          username: meetuAdmin
#          password: popsonfonmeetu
  cloud:
    config:
      uri: ${spring_cloud_config:http://localhost:8585}
      retry:
        max-attempts: 8
        max-interval: 2000
        initial-interval: 10000
      request-read-timeout: 10000
elasticsearch:
    host: ${elastic_host:localhost}
    port: 9200
    
#eureka:
#  instance:
#    appname: eventservice
#  client:
#    service-url.defaultZone: ${eureka_server:http://localhost:8761/eureka}
#    

